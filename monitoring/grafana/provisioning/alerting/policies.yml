apiVersion: 1

# Contact point 를 UI에서 만든 뒤 receiver 이름을 여기에 적으세요.
# 예: receiver: slack-dgst
policies:
  - orgId: 1
    receiver: grafana-default-email
    group_by:
      - alertname
      - severity
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
