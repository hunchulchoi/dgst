apiVersion: 1

# receiver 는 contact_points.yml 의 name 과 동일
policies:
  - orgId: 1
    receiver: telegram-dgst
    group_by:
      - alertname
      - severity
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
